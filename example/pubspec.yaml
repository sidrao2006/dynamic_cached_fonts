name: dynamic_cached_fonts_example
description: An example app to demonstrate the use of dynamic_cached_fonts.

publish_to: 'none'
version: 1.0.0

dependencies:
  flutter:
    sdk: flutter

  dynamic_cached_fonts:
    path: ../
  firebase_core: ^3.0.0
  firebase_storage: ^12.1.0
  async: ^2.8.2

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.20.0 <4.0.0"

flutter:
  uses-material-design: true
